class <%= migration_class_name %> < ActiveRecord::Migration<%= migration_version %>
  def change
    enable_extension "vector" unless extension_enabled?("vector")
    add_column :blazer_queries, :embedding, :vector, limit: 1536
    add_index :blazer_queries, :embedding, using: :hnsw, opclass: :vector_cosine_ops
  end
end
